// Inicializa AOS
AOS.init({
    offset: 120, // Ajusta cuándo se activa la animación
    duration: 600, // Duración en milisegundos
    easing: 'ease-in-out', // Tipo de animación
    once: true, // Ejecutar animación solo una vez
});
