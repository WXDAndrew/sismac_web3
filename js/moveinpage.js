

// (function () {
//     // Añadir un listener al botón para hacer scroll a la sección 2
//     // document.getElementById("scrollInfo").addEventListener("click", function () {
//     //     document.getElementById("carousel__modulos").scrollIntoView({ behavior: 'smooth' });
//     // });

//     document.getElementById("scrollInstalaciones").addEventListener("click", function () {
//         document.getElementById("instalaciones__modulo").scrollIntoView({ behavior: 'smooth' });
//     });
// })();
(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollInfo").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("carousel__modulos").scrollIntoView({ behavior: 'smooth' });
    });
})();


(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollInstalaciones").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("instalaciones__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollFichasTecnicas").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("fichastecnicas__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollListaBaseDeRecambios").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("listabasederecambios__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollMantenimiento").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("mantenimiento__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollRegistroDiarioDeTrabajo").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("registrodiariodetrabajo__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollCompras").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("compras__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollInventarios").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("inventarios__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollSeguridad").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("seguridad__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollPanel").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("panel__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollReportes").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("reportes__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();

(function () {
    // Añadir un listener al botón para hacer scroll a la sección
    document.getElementById("scrollMonitoreo").addEventListener("click", function (event) {
        event.preventDefault(); // Evita el comportamiento predeterminado del enlace
        document.getElementById("monitoreo__modulo").scrollIntoView({ behavior: 'smooth' });
    });
})();



